# pnpm configuration
shamefully-hoist=true

# Note: The warning about @browserbasehq/stagehand missing cli.js is a known
# packaging issue with the stagehand library. The bin script is declared but
# not included in the published package. This doesn't affect functionality.
# The warning can be safely ignored.
